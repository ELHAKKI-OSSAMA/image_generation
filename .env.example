# Environment values consumed by docker-compose.yml.
# Copy this file to `.env` (git-ignored) before running `docker compose up`.
POSTGRES_DB=ofotolab
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me
